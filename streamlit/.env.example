# Database connection type
# Options: 'duckdb' (local) or 'motherduck' (cloud)
DB_TYPE=duckdb

# Required only if DB_TYPE=motherduck
MOTHERDUCK_TOKEN=your_motherduck_token_here

# Required only if DB_TYPE=duckdb
DUCKDB_DATABASE=../noaa-dev.db
